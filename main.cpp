#include <iostream>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, i, a[101];
        cin >> n;
        for (i = 0; i < n; i++)
            cin >> a[i];

        //sort012(a, n);
        cout << endl;
    }

    return 0;
}